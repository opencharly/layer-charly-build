# 2026.254.1251 — skill: sweep the docs-generation skill to the config-driven reality

## Summary

Sweeps the `charly-build:docs` skill to the shipped config-driven generation reality (R2 — the skill's discovery/deployment claims predated the rearchitecture and had diverged). Five claim classes corrected at the source, each verified against the current code (plugin-docs v2026.254.1214, docs @ 3ec7cac6, charly @ d507ca5a, spec @ 57e69f2):

1. **Discovery** -> ONE assembly: `assembleCatalog` unions the remote-aware walk (`CollectEntitiesRemote`) + the compiled corpus (`compiled_plugins` list + go.mod requires via `compiledPluginRef` v0.YYYYDDD.C -> v2026.DDD.CCCC, `refs.DownloadRepo`) + release/extra repos from the config lists — the compiled-in blind spot is closed by construction (previously the skill implied discovery via walked @github refs alone).
2. **Configuration** (new): ONE `docs:` node in the charly repo TOP-LEVEL `charly.yml`, CUE-contract-sourced (`spec/schema/docs.cue` #DocsConfig -> generated `spec.DocsConfig`); absent node = schema defaults; more than one node = error.
3. **Deployment / CI-fidelity** -> the drift-proof recipe: marketplace at the docs-RECORDED gitlink (not main HEAD), the corpus wiring candy (`candy/docs-corpus/charly.yml` from `.github/docs-corpus/pins.tsv` via `build_wiring.py`) written before generate, `--out` under the docs checkout with `astro.config.mjs` present (sidebar gate aborts pre-write without it), fresh/CI-equivalent refs cache.
4. **Path scheme** (new): one `sanitizeSegment` behind every page path AND link target (Astro strips `.`/`:` from slugs) — page path == served URL == link target by construction (the 851-mangled-URL regression the slug cutover fixed).
5. **Tests** -> hermetic-wiring section: self-contained `testdata/{project,marketplace,site}` fixtures, injectable `repoResolver` seams, `CHARLY_DOCS_MARKETPLACE` override; the published corpus's crabbox reference resolves only via the candy-skill complement, so bare-clone tests need fixtures; one deliberate network test remains.

Kept still-true unchanged: out-of-process placement, prescan dispatch, landing projection table, six-entry ownership, link-gate guards, definedness bullets, drift no-op contract, sole-owner deploy.

## How tested

From the pushed tree (branch feat/docs-skill-docs @ 1c537f42):

- Each claim change verified against the referenced sources (commit-level reads, listed above); +145/-10 in `charly.yml` only (the skill entity), clean tree.
- The skill's own renderable content remains consistent (frontmatter description, cross-references intact); no corpus regeneration done in this repo (the marketplace corpus landing regenerates the projection — parent-sequenced).

## Rulebook compliance

- R2 — divergence swept at the source in the current cutover; no parking phrase.
- R3 — the skill documents the ONE assembly; no duplicated path descriptions.
- R6 — no force-push/amend; single-purpose commit.

## Change classification

- **Change class:** documentation (skill entity source).
- **Verification gate:** source-verified claim audit.
- **Attribution tier:** documentation reviewed — the skill entity is the owning repo's doc source; the corpus projection regenerates downstream (named landing: the marketplace regeneration).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (documentation reviewed)*