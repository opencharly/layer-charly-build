# 2026.268.1449 — docs(task): rewire the layer-charly-build skill corpus to the charly task surface

## Summary

Sweep the Taskfile-era task names from the `layer-charly-build` skill sources
(`build`, `migrate`, `validate`). The diff changes exactly three commands:
`task build:binary` → the R9 source build, named as prose (the worktree-local
CalVer-stamped build), with no repo-relative command, `task cue:gen` → `charly task cue-gen`, and
`task mods:tidy` → `charly task mods-tidy`. The sentence that claimed a
superproject `cue:gen` chains plugin param generation is corrected (that wrapper
was deleted before the cutover; each plugin's params generate in its own repo).

Documentation-only: skill-source prose, no runtime/candy behavior.

## How tested

The claim-keyed sweep returns no stale reference in the edited manifest
(`charly.yml`):

```
$ grep -nE '\btask (build:binary|build:install-portable|mods:tidy|cue:gen|wire:gen|proto:gen)\b|Taskfile|taskfiles/' charly.yml
(empty)
```

The edited manifest still parses:

```
$ yq -e '.' charly.yml >/dev/null
(exit 0)
```

Every replacement is a live post-cutover surface: the R9 source build,
`charly task cue-gen`, `charly task mods-tidy`.

## Rulebook compliance

- **R5 (delete legacy completely):** the three removed names are swept in one
  commit; the stale superproject-wrapper claim is corrected in the same pass.
- **R4a:** the rebuild is named as a mechanism/entrypoint, not a bare
  repo-relative command.
- **Documentation-only change class.**

## Change classification

- **Change class:** documentation-only (skill-source prose).
- **Verification gate:** claim-keyed sweep empty + YAML parses.
- **Attribution tier:** `documentation reviewed`.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (documentation reviewed)*